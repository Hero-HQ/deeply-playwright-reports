# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: pr-453-e2e.spec.ts >> PR #453: add Partner view toggle on issue detail page >> partner view toggle renders on issue detail page
- Location: tests/pr-453-e2e.spec.ts:77:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText('PR453 partner view toggle test issue').first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByText('PR453 partner view toggle test issue').first()

```

# Test source

```ts
  1   | import { test, expect } from "@playwright/test";
  2   | import { seedTestData, cleanupTestData } from "../helpers/seed";
  3   | 
  4   | const QA_RUN_ID = process.env.QA_RUN_ID || "unknown";
  5   | const BASE_URL = process.env.PLAYWRIGHT_BASE_URL!;
  6   | const AUTH_FILE = "/home/node/.openclaw/qa-workspace/playwright/.auth/partner_a.json";
  7   | 
  8   | const COUPLE_ID = process.env.QA_COUPLE_ID!;
  9   | const PARTNER_A_ID = process.env.QA_PARTNER_A_ID!;
  10  | const PARTNER_B_ID = process.env.QA_PARTNER_B_ID!;
  11  | 
  12  | let issueId: string;
  13  | 
  14  | test.describe("PR #453: add Partner view toggle on issue detail page", () => {
  15  |   test.use({ storageState: AUTH_FILE });
  16  | 
  17  |   test.beforeAll(async () => {
  18  |     const rows = await seedTestData({
  19  |       table: "issues",
  20  |       records: [
  21  |         {
  22  |           title: "PR453 partner view toggle test issue",
  23  |           couple_id: COUPLE_ID,
  24  |           created_by: PARTNER_A_ID,
  25  |         },
  26  |       ],
  27  |       run_id: QA_RUN_ID,
  28  |     });
  29  |     issueId = rows[0].id as string;
  30  |   });
  31  | 
  32  |   test.afterAll(async () => {
  33  |     await cleanupTestData({ run_id: QA_RUN_ID });
  34  |   });
  35  | 
  36  |   test.beforeEach(async ({ page, context }) => {
  37  |     await page.addStyleTag({
  38  |       content: "*,*::before,*::after{animation-duration:0s!important;transition-duration:0s!important}",
  39  |     });
  40  |     await context.route(
  41  |       /(google-analytics|googletagmanager|segment|sentry|hotjar|posthog|intercom)/,
  42  |       (r) => r.abort()
  43  |     );
  44  |   });
  45  | 
  46  |   test("navigates to /issues and renders the issues page", async ({ page }) => {
  47  |     await test.step("go to issues route", async () => {
  48  |       await page.goto(`${BASE_URL}/issues`);
  49  |     });
  50  | 
  51  |     await test.step("verify Issues nav button is present", async () => {
  52  |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 15000 });
  53  |     });
  54  | 
  55  |     await test.step("verify board/list/matrix view controls are visible", async () => {
  56  |       await expect(page.getByRole("button", { name: "Board" }).first()).toBeVisible({ timeout: 15000 });
  57  |       await expect(page.getByRole("button", { name: "List" }).first()).toBeVisible({ timeout: 15000 });
  58  |     });
  59  |   });
  60  | 
  61  |   test("opens issue detail page for seeded issue", async ({ page }) => {
  62  |     await test.step("navigate directly to issue detail", async () => {
  63  |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  64  |     });
  65  | 
  66  |     await test.step("page loads without error", async () => {
  67  |       await expect(page).toHaveURL(new RegExp(issueId), { timeout: 15000 });
  68  |     });
  69  | 
  70  |     await test.step("verify issue title is displayed", async () => {
  71  |       await expect(
  72  |         page.getByText("PR453 partner view toggle test issue").first()
  73  |       ).toBeVisible({ timeout: 15000 });
  74  |     });
  75  |   });
  76  | 
  77  |   test("partner view toggle renders on issue detail page", async ({ page }) => {
  78  |     await test.step("navigate to issue detail", async () => {
  79  |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  80  |     });
  81  | 
  82  |     await test.step("wait for page to load", async () => {
> 83  |       await expect(page.getByText("PR453 partner view toggle test issue").first()).toBeVisible({
      |                                                                                    ^ Error: expect(locator).toBeVisible() failed
  84  |         timeout: 15000,
  85  |       });
  86  |     });
  87  | 
  88  |     await test.step("partner view toggle is visible", async () => {
  89  |       const toggleLocator = page
  90  |         .locator("[data-testid='partner-view-toggle'], button:has-text('Partner'), [aria-label*='partner'], [aria-label*='Partner']")
  91  |         .first();
  92  |       await expect(toggleLocator).toBeVisible({ timeout: 15000 });
  93  |     });
  94  |   });
  95  | 
  96  |   test("toggling partner view switches perspective on issue detail", async ({ page }) => {
  97  |     await test.step("navigate to issue detail", async () => {
  98  |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  99  |     });
  100 | 
  101 |     await test.step("wait for page to be ready", async () => {
  102 |       await expect(page.getByText("PR453 partner view toggle test issue").first()).toBeVisible({
  103 |         timeout: 15000,
  104 |       });
  105 |     });
  106 | 
  107 |     await test.step("mock any partner view API calls", async () => {
  108 |       await page.route(`**/api/issues/${issueId}/**`, (r) =>
  109 |         r.fulfill({
  110 |           status: 200,
  111 |           contentType: "application/json",
  112 |           body: JSON.stringify({ ok: true }),
  113 |         })
  114 |       );
  115 |     });
  116 | 
  117 |     await test.step("click partner view toggle", async () => {
  118 |       const toggle = page
  119 |         .locator(
  120 |           "[data-testid='partner-view-toggle'], button:has-text('Partner'), [aria-label*='partner' i]"
  121 |         )
  122 |         .first();
  123 |       await expect(toggle).toBeVisible({ timeout: 15000 });
  124 |       await toggle.click();
  125 |     });
  126 | 
  127 |     await test.step("verify view has switched (partner name or label visible)", async () => {
  128 |       const partnerIndicator = page
  129 |         .locator(`text=${PARTNER_B_ID}`, )
  130 |         .or(page.locator("[data-testid='partner-view-active']"))
  131 |         .or(page.locator("text=Partner View"))
  132 |         .first();
  133 |       await expect(partnerIndicator).toBeVisible({ timeout: 15000 });
  134 |     });
  135 |   });
  136 | 
  137 |   test("Work through this button is present on issues page", async ({ page }) => {
  138 |     await test.step("go to issues", async () => {
  139 |       await page.goto(`${BASE_URL}/issues`);
  140 |     });
  141 | 
  142 |     await test.step("verify Work through this button is visible", async () => {
  143 |       await expect(
  144 |         page.getByRole("button", { name: "Work through this" }).first()
  145 |       ).toBeVisible({ timeout: 15000 });
  146 |     });
  147 |   });
  148 | });
```