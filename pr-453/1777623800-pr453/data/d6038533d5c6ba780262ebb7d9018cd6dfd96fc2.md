# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: pr-453-e2e.spec.ts >> PR #453: add Partner view toggle on issue detail page >> issue detail page still shows issue content with toggle present
- Location: tests/pr-453-e2e.spec.ts:105:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: 'Work through this' }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('button', { name: 'Work through this' }).first()

```

# Test source

```ts
  21  |         {
  22  |           title: "QA Partner Toggle Issue",
  23  |           couple_id: COUPLE_ID,
  24  |           created_by: PARTNER_A_ID,
  25  |         },
  26  |       ],
  27  |       run_id: QA_RUN_ID,
  28  |     });
  29  |     issueId = rows[0].id as string;
  30  |   });
  31  | 
  32  |   test.afterAll(async () => {
  33  |     await cleanupTestData({ run_id: QA_RUN_ID });
  34  |   });
  35  | 
  36  |   test.beforeEach(async ({ page, context }) => {
  37  |     await page.addStyleTag({
  38  |       content:
  39  |         "*,*::before,*::after{animation-duration:0s!important;transition-duration:0s!important}",
  40  |     });
  41  |     await context.route(
  42  |       /(google-analytics|googletagmanager|segment|sentry|hotjar|posthog|intercom)/,
  43  |       (r) => r.abort()
  44  |     );
  45  |   });
  46  | 
  47  |   test("navigates to issues list and issue detail page loads", async ({
  48  |     page,
  49  |   }) => {
  50  |     await test.step("navigate to /issues", async () => {
  51  |       await page.goto(`${BASE_URL}/issues`);
  52  |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({
  53  |         timeout: 15000,
  54  |       });
  55  |     });
  56  | 
  57  |     await test.step("navigate to seeded issue detail", async () => {
  58  |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  59  |       await expect(page.getByText("QA Partner Toggle Issue").first()).toBeVisible({
  60  |         timeout: 15000,
  61  |       });
  62  |     });
  63  |   });
  64  | 
  65  |   test("partner view toggle is visible on issue detail page", async ({
  66  |     page,
  67  |   }) => {
  68  |     await test.step("navigate to seeded issue detail", async () => {
  69  |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  70  |     });
  71  | 
  72  |     await test.step("assert partner view toggle is rendered", async () => {
  73  |       // The toggle is expected to exist — look for a button or labelled element
  74  |       const toggle = page
  75  |         .getByRole("button", { name: /partner/i })
  76  |         .first();
  77  |       await expect(toggle).toBeVisible({ timeout: 15000 });
  78  |     });
  79  |   });
  80  | 
  81  |   test("partner view toggle switches between partner perspectives", async ({
  82  |     page,
  83  |   }) => {
  84  |     await test.step("navigate to seeded issue detail", async () => {
  85  |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  86  |     });
  87  | 
  88  |     await test.step("click partner view toggle", async () => {
  89  |       const toggle = page
  90  |         .getByRole("button", { name: /partner/i })
  91  |         .first();
  92  |       await expect(toggle).toBeVisible({ timeout: 15000 });
  93  |       await toggle.click();
  94  |     });
  95  | 
  96  |     await test.step("verify view has changed to partner perspective", async () => {
  97  |       // After toggling, a different partner label or indicator should appear
  98  |       const partnerIndicator = page
  99  |         .getByText(new RegExp(PARTNER_B_ID.slice(0, 8), "i"))
  100 |         .or(page.getByRole("button", { name: /partner/i }).first());
  101 |       await expect(partnerIndicator).toBeVisible({ timeout: 15000 });
  102 |     });
  103 |   });
  104 | 
  105 |   test("issue detail page still shows issue content with toggle present", async ({
  106 |     page,
  107 |   }) => {
  108 |     await test.step("navigate to seeded issue detail", async () => {
  109 |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  110 |     });
  111 | 
  112 |     await test.step("assert issue title is visible", async () => {
  113 |       await expect(
  114 |         page.getByText("QA Partner Toggle Issue").first()
  115 |       ).toBeVisible({ timeout: 15000 });
  116 |     });
  117 | 
  118 |     await test.step("assert work through this action button is visible", async () => {
  119 |       await expect(
  120 |         page.getByRole("button", { name: "Work through this" }).first()
> 121 |       ).toBeVisible({ timeout: 15000 });
      |         ^ Error: expect(locator).toBeVisible() failed
  122 |     });
  123 |   });
  124 | 
  125 |   test("mocks partner view update API call and verifies no crash", async ({
  126 |     page,
  127 |   }) => {
  128 |     await test.step("mock PATCH/PUT for issue updates", async () => {
  129 |       await page.route(`**/api/issues/**`, (r) => {
  130 |         if (r.request().method() === "PATCH" || r.request().method() === "PUT") {
  131 |           r.fulfill({
  132 |             status: 200,
  133 |             contentType: "application/json",
  134 |             body: JSON.stringify({ ok: true, id: issueId }),
  135 |           });
  136 |         } else {
  137 |           r.continue();
  138 |         }
  139 |       });
  140 |     });
  141 | 
  142 |     await test.step("navigate to seeded issue detail", async () => {
  143 |       await page.goto(`${BASE_URL}/issues/${issueId}`);
  144 |       await expect(
  145 |         page.getByText("QA Partner Toggle Issue").first()
  146 |       ).toBeVisible({ timeout: 15000 });
  147 |     });
  148 | 
  149 |     await test.step("interact with partner toggle if present", async () => {
  150 |       const toggle = page
  151 |         .getByRole("button", { name: /partner/i })
  152 |         .first();
  153 |       const isVisible = await toggle.isVisible();
  154 |       if (isVisible) {
  155 |         await toggle.click();
  156 |         // Ensure page does not crash after toggling
  157 |         await expect(
  158 |           page.getByText("QA Partner Toggle Issue").first()
  159 |         ).toBeVisible({ timeout: 15000 });
  160 |       }
  161 |     });
  162 |   });
  163 | });
```