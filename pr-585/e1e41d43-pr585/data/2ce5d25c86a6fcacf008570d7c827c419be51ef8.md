# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: pr-585-e2e.spec.ts >> PR #585: animated radar chart — 7 relationship dimensions on assessment results page >> dimension score labels and status text are displayed correctly
- Location: tests/pr-585-e2e.spec.ts:112:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: /Wholeness.*Strong/ }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('button', { name: /Wholeness.*Strong/ }).first()

```

# Test source

```ts
  19  | 
  20  |   test.afterAll(async () => {
  21  |     await cleanupTestData({ run_id: QA_RUN_ID });
  22  |   });
  23  | 
  24  |   test.beforeEach(async ({ page, context }) => {
  25  |     await page.addStyleTag({
  26  |       content: "*,*::before,*::after{animation-duration:0s!important;transition-duration:0s!important}",
  27  |     });
  28  |     await context.route(
  29  |       /(google-analytics|googletagmanager|segment|sentry|hotjar|posthog|intercom)/,
  30  |       (r) => r.abort()
  31  |     );
  32  |   });
  33  | 
  34  |   test("assessment results page loads and radar chart SVG is visible", async ({ page }) => {
  35  |     await test.step("navigate to assessment results", async () => {
  36  |       await page.goto(`${BASE_URL}/assessment/results`);
  37  |     });
  38  | 
  39  |     await test.step("verify radar chart SVG is rendered", async () => {
  40  |       const radarChart = page.getByLabel("Relationship dimensions radar chart").first();
  41  |       await expect(radarChart).toBeVisible({ timeout: 15000 });
  42  |     });
  43  |   });
  44  | 
  45  |   test("all 7 relationship dimension buttons are visible on results page", async ({ page }) => {
  46  |     await test.step("navigate to assessment results", async () => {
  47  |       await page.goto(`${BASE_URL}/assessment/results`);
  48  |     });
  49  | 
  50  |     await test.step("verify Wholeness dimension button", async () => {
  51  |       const btn = page.getByRole("button", { name: /Wholeness/ }).first();
  52  |       await expect(btn).toBeVisible({ timeout: 15000 });
  53  |     });
  54  | 
  55  |     await test.step("verify Connection dimension button", async () => {
  56  |       const btn = page.getByRole("button", { name: /Connection/ }).first();
  57  |       await expect(btn).toBeVisible({ timeout: 15000 });
  58  |     });
  59  | 
  60  |     await test.step("verify Partnership dimension button", async () => {
  61  |       const btn = page.getByRole("button", { name: /Partnership/ }).first();
  62  |       await expect(btn).toBeVisible({ timeout: 15000 });
  63  |     });
  64  | 
  65  |     await test.step("verify Boundaries dimension button", async () => {
  66  |       const btn = page.getByRole("button", { name: /Boundaries/ }).first();
  67  |       await expect(btn).toBeVisible({ timeout: 15000 });
  68  |     });
  69  | 
  70  |     await test.step("verify Alignment dimension button", async () => {
  71  |       const btn = page.getByRole("button", { name: /Alignment/ }).first();
  72  |       await expect(btn).toBeVisible({ timeout: 15000 });
  73  |     });
  74  | 
  75  |     await test.step("verify Repair dimension button", async () => {
  76  |       const btn = page.getByRole("button", { name: /Repair/ }).first();
  77  |       await expect(btn).toBeVisible({ timeout: 15000 });
  78  |     });
  79  | 
  80  |     await test.step("verify Climate dimension button", async () => {
  81  |       const btn = page.getByRole("button", { name: /Climate/ }).first();
  82  |       await expect(btn).toBeVisible({ timeout: 15000 });
  83  |     });
  84  |   });
  85  | 
  86  |   test("dimension buttons have correct aria-labels with scores", async ({ page }) => {
  87  |     await test.step("navigate to assessment results", async () => {
  88  |       await page.goto(`${BASE_URL}/assessment/results`);
  89  |     });
  90  | 
  91  |     await test.step("verify aria-label for Individual Wholeness — 7 out of 10", async () => {
  92  |       const btn = page.getByLabel("Individual Wholeness — 7 out of 10").first();
  93  |       await expect(btn).toBeVisible({ timeout: 15000 });
  94  |     });
  95  | 
  96  |     await test.step("verify aria-label for Connection — 6 out of 10", async () => {
  97  |       const btn = page.getByLabel("Connection — 6 out of 10").first();
  98  |       await expect(btn).toBeVisible({ timeout: 15000 });
  99  |     });
  100 | 
  101 |     await test.step("verify aria-label for Repair & Recovery — 4 out of 10", async () => {
  102 |       const btn = page.getByLabel("Repair & Recovery — 4 out of 10").first();
  103 |       await expect(btn).toBeVisible({ timeout: 15000 });
  104 |     });
  105 | 
  106 |     await test.step("verify aria-label for Boundaries — 8 out of 10", async () => {
  107 |       const btn = page.getByLabel("Boundaries — 8 out of 10").first();
  108 |       await expect(btn).toBeVisible({ timeout: 15000 });
  109 |     });
  110 |   });
  111 | 
  112 |   test("dimension score labels and status text are displayed correctly", async ({ page }) => {
  113 |     await test.step("navigate to assessment results", async () => {
  114 |       await page.goto(`${BASE_URL}/assessment/results`);
  115 |     });
  116 | 
  117 |     await test.step("verify Wholeness shows score and Strong status", async () => {
  118 |       const btn = page.getByRole("button", { name: /Wholeness.*Strong/ }).first();
> 119 |       await expect(btn).toBeVisible({ timeout: 15000 });
      |                         ^ Error: expect(locator).toBeVisible() failed
  120 |     });
  121 | 
  122 |     await test.step("verify Repair shows Needs work status", async () => {
  123 |       const btn = page.getByRole("button", { name: /Repair.*Needs work/ }).first();
  124 |       await expect(btn).toBeVisible({ timeout: 15000 });
  125 |     });
  126 | 
  127 |     await test.step("verify Connection shows Growing status", async () => {
  128 |       const btn = page.getByRole("button", { name: /Connection.*Growing/ }).first();
  129 |       await expect(btn).toBeVisible({ timeout: 15000 });
  130 |     });
  131 |   });
  132 | 
  133 |   test("back to dashboard link and navigation are accessible on results page", async ({ page }) => {
  134 |     await test.step("navigate to assessment results", async () => {
  135 |       await page.goto(`${BASE_URL}/assessment/results`);
  136 |     });
  137 | 
  138 |     await test.step("verify back to dashboard link is present", async () => {
  139 |       const backLink = page.getByLabel("Back to dashboard").first();
  140 |       await expect(backLink).toBeVisible({ timeout: 15000 });
  141 |     });
  142 | 
  143 |     await test.step("verify Deeply nav link is present", async () => {
  144 |       const navLink = page.getByRole("link", { name: "Deeply" }).first();
  145 |       await expect(navLink).toBeVisible({ timeout: 15000 });
  146 |     });
  147 | 
  148 |     await test.step("verify radar chart is still rendered after nav inspection", async () => {
  149 |       const radarChart = page.getByLabel("Relationship dimensions radar chart").first();
  150 |       await expect(radarChart).toBeVisible({ timeout: 15000 });
  151 |     });
  152 |   });
  153 | });
```