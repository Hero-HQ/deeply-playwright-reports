# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: pr-500-e2e.spec.ts >> PR #500: decode unicode escapes in all AskUser text surfaces >> chat send button is accessible and functional
- Location: tests/pr-500-e2e.spec.ts:93:7

# Error details

```
TimeoutError: locator.fill: Timeout 15000ms exceeded.
Call log:
  - waiting for getByPlaceholder('It\'s okay to start in the middle…').first()

```

# Test source

```ts
  18  |       table: "issues",
  19  |       records: [
  20  |         {
  21  |           title: "Unicode test issue \u2014 em dash & \u00e9",
  22  |           couple_id: COUPLE_ID,
  23  |           created_by: PARTNER_A_ID,
  24  |         },
  25  |       ],
  26  |       run_id: QA_RUN_ID,
  27  |     });
  28  |     seededIssueId = rows[0].id as string;
  29  |   });
  30  | 
  31  |   test.afterAll(async () => {
  32  |     await cleanupTestData({ run_id: QA_RUN_ID });
  33  |   });
  34  | 
  35  |   test.beforeEach(async ({ page, context }) => {
  36  |     await page.addStyleTag({
  37  |       content: "*,*::before,*::after{animation-duration:0s!important;transition-duration:0s!important}",
  38  |     });
  39  |     await context.route(
  40  |       /(google-analytics|googletagmanager|segment|sentry|hotjar|posthog|intercom)/,
  41  |       (r) => r.abort()
  42  |     );
  43  |   });
  44  | 
  45  |   test("chat page loads and textarea is visible", async ({ page }) => {
  46  |     await test.step("navigate to /chat", async () => {
  47  |       await page.goto(`${BASE_URL}/chat`);
  48  |     });
  49  | 
  50  |     await test.step("verify chat textarea is visible", async () => {
  51  |       const textarea = page.getByPlaceholder("It's okay to start in the middle…").first();
  52  |       await expect(textarea).toBeVisible({ timeout: 15000 });
  53  |     });
  54  |   });
  55  | 
  56  |   test("AskUser message with unicode escape sequences renders decoded text in chat", async ({ page }) => {
  57  |     const unicodeBody = JSON.stringify({
  58  |       id: "mock-msg-1",
  59  |       role: "assistant",
  60  |       content: "What do you think about \\u201copen communication\\u201d?",
  61  |     });
  62  | 
  63  |     await test.step("mock chat API to return unicode-escaped AskUser message", async () => {
  64  |       await page.route("**/api/chat**", (r) => {
  65  |         if (r.request().method() === "POST") {
  66  |           r.fulfill({
  67  |             status: 200,
  68  |             contentType: "application/json",
  69  |             body: unicodeBody,
  70  |           });
  71  |         } else {
  72  |           r.continue();
  73  |         }
  74  |       });
  75  |     });
  76  | 
  77  |     await test.step("navigate to /chat", async () => {
  78  |       await page.goto(`${BASE_URL}/chat`);
  79  |     });
  80  | 
  81  |     await test.step("textarea is present", async () => {
  82  |       const textarea = page.getByPlaceholder("It's okay to start in the middle…").first();
  83  |       await expect(textarea).toBeVisible({ timeout: 15000 });
  84  |     });
  85  | 
  86  |     await test.step("type and send a message", async () => {
  87  |       const textarea = page.getByPlaceholder("It's okay to start in the middle…").first();
  88  |       await textarea.fill("Hello");
  89  |       await page.getByRole("button", { name: "Send message" }).first().click();
  90  |     });
  91  |   });
  92  | 
  93  |   test("chat send button is accessible and functional", async ({ page }) => {
  94  |     await test.step("mock POST to chat API", async () => {
  95  |       await page.route("**/api/chat**", (r) => {
  96  |         if (r.request().method() === "POST") {
  97  |           r.fulfill({
  98  |             status: 200,
  99  |             contentType: "application/json",
  100 |             body: '{"ok":true}',
  101 |           });
  102 |         } else {
  103 |           r.continue();
  104 |         }
  105 |       });
  106 |     });
  107 | 
  108 |     await test.step("navigate to /chat", async () => {
  109 |       await page.goto(`${BASE_URL}/chat`);
  110 |     });
  111 | 
  112 |     await test.step("send button is visible", async () => {
  113 |       await expect(page.getByRole("button", { name: "Send message" }).first()).toBeVisible({ timeout: 15000 });
  114 |     });
  115 | 
  116 |     await test.step("type a message and click send", async () => {
  117 |       const textarea = page.getByPlaceholder("It's okay to start in the middle…").first();
> 118 |       await textarea.fill("Test unicode: \u00e9\u00e0\u00fc");
      |                      ^ TimeoutError: locator.fill: Timeout 15000ms exceeded.
  119 |       await page.getByRole("button", { name: "Send message" }).first().click();
  120 |     });
  121 |   });
  122 | 
  123 |   test("issues page loads and Work through this button is visible", async ({ page }) => {
  124 |     await test.step("navigate to /issues", async () => {
  125 |       await page.goto(`${BASE_URL}/issues`);
  126 |     });
  127 | 
  128 |     await test.step("Work through this button is visible", async () => {
  129 |       await expect(page.getByRole("button", { name: "Work through this" }).first()).toBeVisible({ timeout: 15000 });
  130 |     });
  131 |   });
  132 | 
  133 |   test("Work through this on issues page navigates to chat with decoded unicode in AskUser prompt", async ({ page }) => {
  134 |     await test.step("mock chat conversation API for unicode response", async () => {
  135 |       await page.route("**/api/conversations/**", (r) => {
  136 |         if (r.request().method() === "POST") {
  137 |           r.fulfill({
  138 |             status: 200,
  139 |             contentType: "application/json",
  140 |             body: JSON.stringify({
  141 |               id: process.env.QA_CONVERSATION_ID,
  142 |               title: "Issue conversation",
  143 |               couple_id: COUPLE_ID,
  144 |             }),
  145 |           });
  146 |         } else {
  147 |           r.continue();
  148 |         }
  149 |       });
  150 |     });
  151 | 
  152 |     await test.step("navigate to /issues", async () => {
  153 |       await page.goto(`${BASE_URL}/issues`);
  154 |     });
  155 | 
  156 |     await test.step("click Work through this", async () => {
  157 |       await expect(page.getByRole("button", { name: "Work through this" }).first()).toBeVisible({ timeout: 15000 });
  158 |       await page.getByRole("button", { name: "Work through this" }).first().click();
  159 |     });
  160 | 
  161 |     await test.step("verify navigation to chat or chat UI elements appear", async () => {
  162 |       await expect
  163 |         .poll(
  164 |           async () => {
  165 |             const url = page.url();
  166 |             const hasTextarea = await page.getByPlaceholder("It's okay to start in the middle…").first().isVisible().catch(() => false);
  167 |             return url.includes("/chat") || hasTextarea;
  168 |           },
  169 |           { timeout: 15000 }
  170 |         )
  171 |         .toBeTruthy();
  172 |     });
  173 |   });
  174 | });
```