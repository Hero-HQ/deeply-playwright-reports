# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: pr-435-e2e.spec.ts >> PR #435: resolution mechanics (propose / confirm / reopen) >> propose-resolution API is called and mocked correctly on issue detail
- Location: tests/pr-435-e2e.spec.ts:44:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: 'Issues' }).first()
Expected: visible
Timeout: 10000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 10000ms
  - waiting for getByRole('button', { name: 'Issues' }).first()

```

# Test source

```ts
  1   | import { test, expect } from "@playwright/test";
  2   | import { seedTestData, cleanupTestData } from "../helpers/seed";
  3   | 
  4   | const QA_RUN_ID = process.env.QA_RUN_ID || "unknown";
  5   | const BASE_URL = process.env.PLAYWRIGHT_BASE_URL!;
  6   | const AUTH_FILE = "/home/node/.openclaw/qa-workspace/playwright/.auth/partner_a.json";
  7   | 
  8   | const ISSUE_ID = "ecf5d9c2-5466-46d7-90f0-649a8d55526d";
  9   | 
  10  | test.describe("PR #435: resolution mechanics (propose / confirm / reopen)", () => {
  11  |   test.use({ storageState: AUTH_FILE });
  12  | 
  13  |   test.beforeAll(async () => {
  14  |     await seedTestData({ run_id: QA_RUN_ID });
  15  |   });
  16  | 
  17  |   test.afterAll(async () => {
  18  |     await cleanupTestData({ run_id: QA_RUN_ID });
  19  |   });
  20  | 
  21  |   test.beforeEach(async ({ page, context }) => {
  22  |     await page.addStyleTag({ content: "*,*::before,*::after{animation-duration:0s!important;transition-duration:0s!important}" });
  23  |     await context.route(/(google-analytics|googletagmanager|segment|sentry|hotjar|posthog|intercom)/, r => r.abort());
  24  |   });
  25  | 
  26  |   test("issues list page loads with view toggle buttons", async ({ page }) => {
  27  |     await test.step("navigate to /issues", async () => {
  28  |       await page.goto(`${BASE_URL}/issues`);
  29  |     });
  30  | 
  31  |     await test.step("verify Board view toggle is visible", async () => {
  32  |       await expect(page.getByRole("button", { name: "Board" }).first()).toBeVisible({ timeout: 10000 });
  33  |     });
  34  | 
  35  |     await test.step("verify List view toggle is visible", async () => {
  36  |       await expect(page.getByRole("button", { name: "List" }).first()).toBeVisible({ timeout: 10000 });
  37  |     });
  38  | 
  39  |     await test.step("verify Matrix view toggle is visible", async () => {
  40  |       await expect(page.getByRole("button", { name: "Matrix" }).first()).toBeVisible({ timeout: 10000 });
  41  |     });
  42  |   });
  43  | 
  44  |   test("propose-resolution API is called and mocked correctly on issue detail", async ({ page }) => {
  45  |     await test.step("mock propose-resolution endpoint", async () => {
  46  |       await page.route(`**/api/issues/${ISSUE_ID}/propose-resolution`, r =>
  47  |         r.fulfill({ status: 200, contentType: "application/json", body: '{"ok":true}' })
  48  |       );
  49  |     });
  50  | 
  51  |     await test.step("navigate to issue detail page", async () => {
  52  |       await page.goto(`${BASE_URL}/issues/${ISSUE_ID}`);
  53  |     });
  54  | 
  55  |     await test.step("verify issues page navigation item is present", async () => {
> 56  |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 10000 });
      |                                                                          ^ Error: expect(locator).toBeVisible() failed
  57  |     });
  58  | 
  59  |     await test.step("trigger propose-resolution via fetch and verify mock responded", async () => {
  60  |       const [response] = await Promise.all([
  61  |         page.waitForResponse(`**/api/issues/${ISSUE_ID}/propose-resolution`),
  62  |         page.evaluate((issueId) => {
  63  |           return fetch(`/api/issues/${issueId}/propose-resolution`, {
  64  |             method: "POST",
  65  |             headers: { "Content-Type": "application/json" },
  66  |             body: JSON.stringify({ note: "test resolution note" }),
  67  |           });
  68  |         }, ISSUE_ID),
  69  |       ]);
  70  |       expect(response.status()).toBe(200);
  71  |     });
  72  |   });
  73  | 
  74  |   test("confirm-resolution API is mocked and returns success", async ({ page }) => {
  75  |     await test.step("mock confirm-resolution endpoint", async () => {
  76  |       await page.route(`**/api/issues/${ISSUE_ID}/confirm-resolution`, r =>
  77  |         r.fulfill({ status: 200, contentType: "application/json", body: '{"ok":true}' })
  78  |       );
  79  |     });
  80  | 
  81  |     await test.step("navigate to issues page", async () => {
  82  |       await page.goto(`${BASE_URL}/issues`);
  83  |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 10000 });
  84  |     });
  85  | 
  86  |     await test.step("call confirm-resolution API and verify mock responds with ok", async () => {
  87  |       const [response] = await Promise.all([
  88  |         page.waitForResponse(`**/api/issues/${ISSUE_ID}/confirm-resolution`),
  89  |         page.evaluate((issueId) => {
  90  |           return fetch(`/api/issues/${issueId}/confirm-resolution`, {
  91  |             method: "POST",
  92  |             headers: { "Content-Type": "application/json" },
  93  |             body: JSON.stringify({}),
  94  |           });
  95  |         }, ISSUE_ID),
  96  |       ]);
  97  |       expect(response.status()).toBe(200);
  98  |       const body = await response.json();
  99  |       expect(body.ok).toBe(true);
  100 |     });
  101 |   });
  102 | 
  103 |   test("reopen API is mocked and returns success", async ({ page }) => {
  104 |     await test.step("mock reopen endpoint", async () => {
  105 |       await page.route(`**/api/issues/${ISSUE_ID}/reopen`, r =>
  106 |         r.fulfill({ status: 200, contentType: "application/json", body: '{"ok":true}' })
  107 |       );
  108 |     });
  109 | 
  110 |     await test.step("navigate to issues page", async () => {
  111 |       await page.goto(`${BASE_URL}/issues`);
  112 |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 10000 });
  113 |     });
  114 | 
  115 |     await test.step("call reopen API and verify mock responds with ok", async () => {
  116 |       const [response] = await Promise.all([
  117 |         page.waitForResponse(`**/api/issues/${ISSUE_ID}/reopen`),
  118 |         page.evaluate((issueId) => {
  119 |           return fetch(`/api/issues/${issueId}/reopen`, {
  120 |             method: "POST",
  121 |             headers: { "Content-Type": "application/json" },
  122 |             body: JSON.stringify({ reason: "test reopen reason" }),
  123 |           });
  124 |         }, ISSUE_ID),
  125 |       ]);
  126 |       expect(response.status()).toBe(200);
  127 |       const body = await response.json();
  128 |       expect(body.ok).toBe(true);
  129 |     });
  130 |   });
  131 | 
  132 |   test("sidebar navigation is present and functional on issues page", async ({ page }) => {
  133 |     await test.step("navigate to /issues", async () => {
  134 |       await page.goto(`${BASE_URL}/issues`);
  135 |     });
  136 | 
  137 |     await test.step("verify Issues nav button is visible", async () => {
  138 |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 10000 });
  139 |     });
  140 | 
  141 |     await test.step("verify Tasks nav button is visible", async () => {
  142 |       await expect(page.getByRole("button", { name: "Tasks" }).first()).toBeVisible({ timeout: 10000 });
  143 |     });
  144 | 
  145 |     await test.step("click Tasks nav button and verify navigation", async () => {
  146 |       await page.getByRole("button", { name: "Tasks" }).first().click();
  147 |       await expect(page.getByRole("button", { name: "Tasks" }).first()).toBeVisible({ timeout: 10000 });
  148 |     });
  149 | 
  150 |     await test.step("navigate back to issues and verify Reminders nav button", async () => {
  151 |       await page.goto(`${BASE_URL}/issues`);
  152 |       await expect(page.getByRole("button", { name: "Reminders" }).first()).toBeVisible({ timeout: 10000 });
  153 |     });
  154 |   });
  155 | });
```