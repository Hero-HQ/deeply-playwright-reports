# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: pr-435-e2e.spec.ts >> PR #435: resolution mechanics — propose / confirm / reopen >> reopen issue — POST is mocked and does not pollute QA data
- Location: tests/pr-435-e2e.spec.ts:116:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: 'Issues' }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('button', { name: 'Issues' }).first()

```

# Test source

```ts
  28  |   });
  29  | 
  30  |   test("issues list page loads with Board/List/Matrix view toggles", async ({ page }) => {
  31  |     await test.step("navigate to /issues", async () => {
  32  |       await page.goto(`${BASE_URL}/issues`);
  33  |     });
  34  | 
  35  |     await test.step("verify Board view button is visible", async () => {
  36  |       await expect(page.getByRole("button", { name: "Board" }).first()).toBeVisible({ timeout: 15000 });
  37  |     });
  38  | 
  39  |     await test.step("verify List view button is visible", async () => {
  40  |       await expect(page.getByRole("button", { name: "List" }).first()).toBeVisible({ timeout: 10000 });
  41  |     });
  42  | 
  43  |     await test.step("verify Matrix view button is visible", async () => {
  44  |       await expect(page.getByRole("button", { name: "Matrix" }).first()).toBeVisible({ timeout: 10000 });
  45  |     });
  46  | 
  47  |     await test.step("verify Issues nav button is visible", async () => {
  48  |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 10000 });
  49  |     });
  50  |   });
  51  | 
  52  |   test("propose resolution — POST is mocked and does not pollute QA data", async ({ page, context }) => {
  53  |     await test.step("mock propose-resolution endpoint", async () => {
  54  |       await page.route(`**/api/issues/${ISSUE_ID}/propose-resolution`, r =>
  55  |         r.fulfill({ status: 200, contentType: "application/json", body: '{"ok":true}' })
  56  |       );
  57  |     });
  58  | 
  59  |     await test.step("navigate to issue detail page", async () => {
  60  |       await page.goto(`${BASE_URL}/issues/${ISSUE_ID}`);
  61  |     });
  62  | 
  63  |     await test.step("verify page loaded (Issues nav visible)", async () => {
  64  |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 15000 });
  65  |     });
  66  | 
  67  |     await test.step("fire mocked propose-resolution POST", async () => {
  68  |       const [response] = await Promise.all([
  69  |         page.waitForResponse(resp => resp.url().includes(`/api/issues/${ISSUE_ID}/propose-resolution`), { timeout: 5000 }).catch(() => null),
  70  |         page.evaluate(async (issueId) => {
  71  |           await fetch(`/api/issues/${issueId}/propose-resolution`, {
  72  |             method: "POST",
  73  |             headers: { "Content-Type": "application/json" },
  74  |             body: JSON.stringify({ note: "E2E test proposal" }),
  75  |           });
  76  |         }, ISSUE_ID),
  77  |       ]);
  78  |       if (response) {
  79  |         expect(response.status()).toBe(200);
  80  |       }
  81  |     });
  82  |   });
  83  | 
  84  |   test("confirm resolution — POST is mocked and does not pollute QA data", async ({ page }) => {
  85  |     await test.step("mock confirm-resolution endpoint", async () => {
  86  |       await page.route(`**/api/issues/${ISSUE_ID}/confirm-resolution`, r =>
  87  |         r.fulfill({ status: 200, contentType: "application/json", body: '{"ok":true}' })
  88  |       );
  89  |     });
  90  | 
  91  |     await test.step("navigate to issue detail page", async () => {
  92  |       await page.goto(`${BASE_URL}/issues/${ISSUE_ID}`);
  93  |     });
  94  | 
  95  |     await test.step("verify page loaded (Issues nav visible)", async () => {
  96  |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 15000 });
  97  |     });
  98  | 
  99  |     await test.step("fire mocked confirm-resolution POST", async () => {
  100 |       const [response] = await Promise.all([
  101 |         page.waitForResponse(resp => resp.url().includes(`/api/issues/${ISSUE_ID}/confirm-resolution`), { timeout: 5000 }).catch(() => null),
  102 |         page.evaluate(async (issueId) => {
  103 |           await fetch(`/api/issues/${issueId}/confirm-resolution`, {
  104 |             method: "POST",
  105 |             headers: { "Content-Type": "application/json" },
  106 |             body: JSON.stringify({}),
  107 |           });
  108 |         }, ISSUE_ID),
  109 |       ]);
  110 |       if (response) {
  111 |         expect(response.status()).toBe(200);
  112 |       }
  113 |     });
  114 |   });
  115 | 
  116 |   test("reopen issue — POST is mocked and does not pollute QA data", async ({ page }) => {
  117 |     await test.step("mock reopen endpoint", async () => {
  118 |       await page.route(`**/api/issues/${ISSUE_ID}/reopen`, r =>
  119 |         r.fulfill({ status: 200, contentType: "application/json", body: '{"ok":true}' })
  120 |       );
  121 |     });
  122 | 
  123 |     await test.step("navigate to issue detail page", async () => {
  124 |       await page.goto(`${BASE_URL}/issues/${ISSUE_ID}`);
  125 |     });
  126 | 
  127 |     await test.step("verify page loaded (Issues nav visible)", async () => {
> 128 |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 15000 });
      |                                                                          ^ Error: expect(locator).toBeVisible() failed
  129 |     });
  130 | 
  131 |     await test.step("fire mocked reopen POST", async () => {
  132 |       const [response] = await Promise.all([
  133 |         page.waitForResponse(resp => resp.url().includes(`/api/issues/${ISSUE_ID}/reopen`), { timeout: 5000 }).catch(() => null),
  134 |         page.evaluate(async (issueId) => {
  135 |           await fetch(`/api/issues/${issueId}/reopen`, {
  136 |             method: "POST",
  137 |             headers: { "Content-Type": "application/json" },
  138 |             body: JSON.stringify({ reason: "E2E test reopen" }),
  139 |           });
  140 |         }, ISSUE_ID),
  141 |       ]);
  142 |       if (response) {
  143 |         expect(response.status()).toBe(200);
  144 |       }
  145 |     });
  146 |   });
  147 | 
  148 |   test("dashboard loads and core navigation is functional", async ({ page }) => {
  149 |     await test.step("navigate to /dashboard", async () => {
  150 |       await page.goto(`${BASE_URL}/dashboard`);
  151 |     });
  152 | 
  153 |     await test.step("verify Home nav button is visible", async () => {
  154 |       await expect(page.getByRole("button", { name: "Home" }).first()).toBeVisible({ timeout: 15000 });
  155 |     });
  156 | 
  157 |     await test.step("verify Issues nav button is visible", async () => {
  158 |       await expect(page.getByRole("button", { name: "Issues" }).first()).toBeVisible({ timeout: 10000 });
  159 |     });
  160 | 
  161 |     await test.step("verify Tasks nav button is visible", async () => {
  162 |       await expect(page.getByRole("button", { name: "Tasks" }).first()).toBeVisible({ timeout: 10000 });
  163 |     });
  164 | 
  165 |     await test.step("click Issues nav and verify /issues loads", async () => {
  166 |       await page.getByRole("button", { name: "Issues" }).first().click();
  167 |       await expect(page.getByRole("button", { name: "Board" }).first()).toBeVisible({ timeout: 15000 });
  168 |     });
  169 |   });
  170 | });
```